---
export const prerender = false

// Root redirect — detect language from Accept-Language header or default to 'id'
const acceptLang = Astro.request.headers.get('accept-language') || ''
const preferredLang = acceptLang.toLowerCase().startsWith('en') ? 'en' : 'id'
return Astro.redirect(`/${preferredLang}`)
---
